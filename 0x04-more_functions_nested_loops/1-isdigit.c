#include "main.h"

/**
 * _isupper - check for upppercase character
 * @c: the parameter
 * Return: 0 and 1
 */
int _isdigit(int c);
{
	if (c >= 48 && c <= 57)
	{
		return (1);
	}
	else
	{
		return (0);
	}
	_putchar('\n');
}
