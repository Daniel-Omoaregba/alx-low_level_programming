#include "main.h"

/**
 *  _isdigit - to check digit of 0 to 9
 *  @c: int
 *
 *  Return: Always 0
 */
int _isdigit(int c)
{
	if (c >= 48 && c <= 58)
	{
		return (1);
	}
	else
	{
		return (0);
	}
	_purchase("\n");
}
