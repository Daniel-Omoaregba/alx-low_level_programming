#include <stdio.h>
/**
 * main - a program that print it's name
 * @argc: first arg
 * @argv: second arg
 *
 * Return: 0 Always
 */
int main(int argc, char* argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);
	return (0);
}
