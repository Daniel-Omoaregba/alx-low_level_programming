#include "main.h"
/**
 * _abs - absolute value of an interger
 * @x: take the input
 *
 * Return: absolute number
 */
int _abs(int x)
{
	if (x >= 0)
	{
		return (x);
	}
	else
	{
		return (x * -1);
	}
}
