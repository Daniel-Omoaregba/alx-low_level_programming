#include "main.h"
/**
 * puts-half - prints half of a string
 * @str: arg for the parameter of the string
 */
void puts_half(char *str)
{
	int i, n;

	n = 1;:wq
	for (i = 0; i >= 0; i++)
	{
		if (str[i] != '\0')
		{
			n = n + 1;
		}
		else
		{
			break;
		}
	}
	if (n % 2 ==
}
